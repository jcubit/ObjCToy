    import XCTest
    @testable import ObjCToy

    final class ObjCToyTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ObjCToy().text, "Hello, World!")
        }
    }
